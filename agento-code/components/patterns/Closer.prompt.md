Ends a marketing page on the one inverted panel the design allows. Exactly one per page.

```jsx
<Closer title="Your sessions are already on disk."
  actions={<>
    <Button variant="primary" onInvert href={release}>Download 1.2.0 →</Button>
    <Button variant="ghost" onInvert href={repo}>Star on GitHub</Button>
  </>}>
  Agento just reads them.
</Closer>
```
