Squared mono tag that classifies the row it sits in — a file kind, a section key.

```jsx
<Row badge="dmg" mono="Agento_1.2.0_aarch64.dmg" right="arm64 · 12.4 MB" go="↓" />
<Badge>docs</Badge>
```

Badge is squared and muted; `Pill` is round and is for metadata. Do not use both
on the same row.
