Shell commands and config snippets — one solid dark rectangle, no title bar or window dots.

```jsx
<CodeBlock><CodeKey>claude</CodeKey> --version</CodeBlock>
<CodeBlock code={"chmod +x Agento.AppImage\n./Agento.AppImage"} />
```

Only three highlight roles exist: `CodeKey` (white bold — the command),
`CodeStr` (blue — strings and paths), `CodeComment` (faint — trailing `#` notes).
