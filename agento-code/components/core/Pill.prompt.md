Metadata capsule — version numbers, platforms, licence, tech stack, blog tags.

```jsx
<Pill>v1.2.0</Pill>
<Pill plain>macOS · Windows · Linux</Pill>
```

The filled form marks the one fact worth reading first (the version, "Latest");
everything beside it is `plain`. Separate multiple facts inside one pill with ` · `.
