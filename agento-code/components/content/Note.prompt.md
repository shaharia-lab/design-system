An aside in documentation prose. Dashed for information, amber and solid for a warning.

```jsx
<Note label="Privacy">Everything is stored locally in <code>~/.agento</code>.</Note>
<Note kind="warn">Integration credentials are stored in plain text in the local database.</Note>
```

Amber is the only status colour this brand has — there is no success green.
