The site identity in the masthead. It is type plus a dot — there is no logo image to place.

```jsx
<Wordmark label="Agento" href="/" />
```

`assets/agento-mark.svg` is the app icon / favicon and is the only drawn mark the
brand has; do not substitute it for the wordmark in a header.
