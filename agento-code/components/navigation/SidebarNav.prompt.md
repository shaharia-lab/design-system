Left column of the documentation grid; sticky at `top: 78px`, 232px wide.

```jsx
<SidebarNav current="/docs/installation/" groups={[
  { label: 'Start here', items: [{ label: 'Installation', href: '/docs/installation/' }] },
]} />
```

Group labels are mono uppercase with an ink rule beneath — the same eyebrow
device used on panels.
