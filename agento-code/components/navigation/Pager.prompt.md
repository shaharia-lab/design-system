Closes a docs page with its neighbours in the sidebar order.

```jsx
<Pager prev={{ label: 'Installation', href: '/docs/installation/' }}
       next={{ label: 'User guide', href: '/docs/user-guide/' }} />
```

Either side may be omitted; a lone `next` stays in the right-hand column.
